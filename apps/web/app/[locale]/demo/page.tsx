import type { Metadata } from 'next'; import { notFound } from 'next/navigation';
import { DemoForm } from '../../../src/marketing/components/demo-form'; import { isLocale } from '../../../src/marketing/routing/locales'; import { marketingMetadata } from '../../../src/marketing/seo/metadata';
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; if (!isLocale(locale)) return {}; return locale === 'uk' ? marketingMetadata(locale, '/demo', 'Демо Sales AITO', 'Покажемо, як Sales AITO обробляє звернення та створює замовлення у вашому процесі.') : marketingMetadata(locale, '/demo', 'Sales AITO demo', 'See how Sales AITO processes enquiries and creates orders inside your workflow.'); }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const uk = locale === 'uk'; return <main id="main-content"><header className="content-hero"><div className="marketing-shell content-hero__inner"><h1>{uk ? 'Подивіться Sales AITO на вашому сценарії' : 'See Sales AITO on your workflow'}</h1><p>{uk ? 'Розберемо поточний шлях замовлення, покажемо доступні функції та чесно відділимо їх від roadmap.' : 'We will review your current order journey, show available capabilities and clearly separate them from the roadmap.'}</p></div></header><section className="content-section"><div className="marketing-shell demo-layout"><div><h2>{uk ? 'Що буде на демо' : 'What the demo covers'}</h2><p>{uk ? 'Instagram-діалоги, AI-розпізнавання, перевірка каталогу, створення замовлення та експорт у Google Sheets.' : 'Instagram conversations, AI recognition, catalogue validation, order creation and Google Sheets export.'}</p></div><DemoForm locale={locale} /></div></section></main>; }
