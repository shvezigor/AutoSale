import type { Metadata } from 'next'; import { notFound } from 'next/navigation';
import { StatusBadge } from '../../../src/marketing/components/status-badge'; import { integrations } from '../../../src/marketing/content/integrations'; import { isLocale } from '../../../src/marketing/routing/locales'; import { marketingMetadata } from '../../../src/marketing/seo/metadata';
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; if (!isLocale(locale)) return {}; return locale === 'uk' ? marketingMetadata(locale, '/integrations', 'Інтеграції Sales AITO', 'Доступні, розроблювані та майбутні канали Sales AITO з чесним статусом кожної інтеграції.') : marketingMetadata(locale, '/integrations', 'Sales AITO integrations', 'Available, in-development and roadmap Sales AITO channels with a clear status for every integration.'); }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); return <main id="main-content"><header className="content-hero"><div className="marketing-shell content-hero__inner"><h1>{locale === 'uk' ? 'Усі канали. Один операційний потік.' : 'Every channel. One operating flow.'}</h1><p>{locale === 'uk' ? 'Ми не видаємо roadmap за готовий продукт. Тут видно реальний статус кожного підключення.' : 'We never present roadmap work as a finished product. Every connection has a clear status here.'}</p></div></header><section className="content-section"><div className="marketing-shell"><div className="integration-directory">{integrations.map((item) => <article key={item.id}><h2>{item.name}</h2><StatusBadge locale={locale} status={item.status} /><p>{item.summary[locale]}</p></article>)}</div></div></section></main>; }
