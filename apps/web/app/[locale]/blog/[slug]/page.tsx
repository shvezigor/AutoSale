import type { Metadata } from 'next'; import { notFound } from 'next/navigation';
import { article } from '../../../../src/marketing/content/articles'; import { isLocale } from '../../../../src/marketing/routing/locales'; import { marketingMetadata } from '../../../../src/marketing/seo/metadata';
export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }): Promise<Metadata> { const { locale, slug } = await params; if (!isLocale(locale) || slug !== article.slug) return {}; const c = article[locale]; return marketingMetadata(locale, `/blog/${slug}`, c.title, c.description); }
export default async function Page({ params }: { params: Promise<{ locale: string; slug: string }> }) { const { locale, slug } = await params; if (!isLocale(locale) || slug !== article.slug) notFound(); const c = article[locale]; const jsonLd = { '@context': 'https://schema.org', '@type': 'Article', headline: c.title, description: c.description, author: { '@type': 'Organization', name: 'Sales AITO' }, publisher: { '@type': 'Organization', name: 'Sales AITO' } }; return <main id="main-content"><article><header className="content-hero"><div className="marketing-shell content-hero__inner"><h1>{c.title}</h1><p>{c.description}</p></div></header><div className="content-section"><div className="marketing-shell article-prose">{c.sections.map(([title, body]) => <section key={title}><h2>{title}</h2><p>{body}</p></section>)}</div></div></article><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c') }} /></main>; }
