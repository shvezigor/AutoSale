import type { Metadata } from 'next'; import Link from 'next/link'; import { notFound } from 'next/navigation';
import { article } from '../../../src/marketing/content/articles'; import { isLocale, localizedPath } from '../../../src/marketing/routing/locales'; import { marketingMetadata } from '../../../src/marketing/seo/metadata';
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; if (!isLocale(locale)) return {}; return locale === 'uk' ? marketingMetadata(locale, '/blog', 'Блог про автоматизацію social commerce', 'Практичні матеріали Sales AITO про AI, обробку замовлень та операції продажів у соцмережах.') : marketingMetadata(locale, '/blog', 'Social commerce automation blog', 'Practical Sales AITO guides to AI, order processing and sales operations across social channels.'); }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const c = article[locale]; return <main id="main-content"><header className="content-hero"><div className="marketing-shell content-hero__inner"><h1>{locale === 'uk' ? 'Практика автоматизованих продажів' : 'Practical automated selling'}</h1><p>{locale === 'uk' ? 'Без магічних обіцянок. Про процеси, контроль і реальну цінність AI для social commerce.' : 'No magic promises. Just processes, control and the real value of AI for social commerce.'}</p></div></header><section className="content-section"><div className="marketing-shell article-list"><article><h2><Link href={localizedPath(locale, `/blog/${article.slug}`)}>{c.title}</Link></h2><p>{c.description}</p><Link className="text-link text-link--dark" href={localizedPath(locale, `/blog/${article.slug}`)}>{locale === 'uk' ? 'Читати статтю' : 'Read article'} <span aria-hidden="true">→</span></Link></article></div></section></main>; }
